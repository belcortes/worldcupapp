- @users.each do |user|
  = user.name 
  %br/
  = link_to('delete', user_path(user), method: :delete, confirm: 'Are you sure you want to do this?') if current_user.admin?
  %br/
  = link_to('edit', user_path(user), method: :get) if current_user.admin?
  %br/
  %br/




