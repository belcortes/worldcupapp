%p
  The first match of World Cup 2014 will be held on June 14, 2014 in Fortaleza. 

%p
  The final match of World Cup 2014 will be held on July 13, 2014 in Rio de Janeiro.

= form_for [current_user,@itinerary] do |f|
  = f.label :name
  = f.text_field :name
  = f.label "Arrival date" 
  = f.label "yyyy/mm/dd"
  = f.text_field :start_date, class: "datepicker"  # won't accept invalid inputs including fake dates (i.e. 2013-06-31)
  = f.label :departure_date
  = f.label "yyyy/mm/dd"
  = f.text_field :end_date, class: "datepicker"  # won't accept invalid inputs including fake dates (i.e. 2013-06-31)  

  / - @cities.each do |city|
 
  /   = city.name
  /   %span
  /   = check_box_tag 'itinerary[games][]', game.city_id, city.in?(@itinerary.cities)
  = f.submit

- if @itinerary.errors.any?
  %h6
    There are errors on this form
  %ul
    - @itinerary.errors.full_messages.each do |msg|
      %li
        = msg